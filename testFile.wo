// declare lunatic {
//   fn connect(ptr: i32, len: i32): i32;
// } from "lunatic";
// 

fn someBool(): bool {
  return true;
}

export fn main(): void {
  let a = someBool() && true;
  if (a) {
    someBool();
  }
  return;
}
