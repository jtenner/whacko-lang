// declare lunatic {
//   fn connect(ptr: i32, len: i32): i32;
// } from "lunatic";

class A {
  a: i32 = i32(42);

  constructor() {
  }

  doStuff<U>(a: U): void {
    a + a;
    return;
  }
}

export fn _start(): void {
  let test = new A();
  test.doStuff<i32>(i32(1234));
  return;
}
