@name("__whacko_string_x") extern x(str: String): i32; 

export class String {
  constructor() {
    unreachable();
  }

  length(): u32 {
    return types.sizeOf(this);
  }

  x(): i32 {
    return x(this);
  }
}
