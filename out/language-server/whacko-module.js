"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createWhackoServices = exports.WhackoModule = void 0;
const langium_1 = require("langium");
const module_1 = require("./generated/module");
const whacko_validator_1 = require("./whacko-validator");
/**
 * Dependency injection module that overrides Langium default services and contributes the
 * declared custom services. The Langium defaults can be partially specified to override only
 * selected services, while the custom services must be fully specified.
 */
exports.WhackoModule = {
    validation: {
        WhackoValidator: () => new whacko_validator_1.WhackoValidator(),
    },
};
/**
 * Create the full set of services required by Langium.
 *
 * First inject the shared services by merging two modules:
 *  - Langium default shared services
 *  - Services generated by langium-cli
 *
 * Then inject the language-specific services by merging three modules:
 *  - Langium default language-specific services
 *  - Services generated by langium-cli
 *  - Services specified in this file
 *
 * @param context Optional module context with the LSP connection
 * @returns An object wrapping the shared services and the language-specific services
 */
function createWhackoServices(context) {
    const shared = (0, langium_1.inject)((0, langium_1.createDefaultSharedModule)(context), module_1.WhackoGeneratedSharedModule);
    const Whacko = (0, langium_1.inject)((0, langium_1.createDefaultModule)({ shared }), module_1.WhackoGeneratedModule, exports.WhackoModule);
    shared.ServiceRegistry.register(Whacko);
    (0, whacko_validator_1.registerValidationChecks)(Whacko);
    return { shared, Whacko };
}
exports.createWhackoServices = createWhackoServices;
//# sourceMappingURL=whacko-module.js.map