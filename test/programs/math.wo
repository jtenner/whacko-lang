class Foo {
  someField: i32;
  constructor() {
    this.someField = i32(0);
  }
}

fn bar(): f64 {
  return 2.0;
}

export fn _start(): void {
  let foo = new Foo();
  math.asinf(f32(1.0));
  math.asin(bar() - 1.0);
  heap.free(foo);
}
