class Foo {
  someField: i32;
  constructor() {
    this.someField = 0;
  }
}

export fn _start(): void {
  let foo = new Foo(); // I think we just forgot to set stuff to the map again
  math.asinf(f32(1.0));
  math.asin(1.0);
}
